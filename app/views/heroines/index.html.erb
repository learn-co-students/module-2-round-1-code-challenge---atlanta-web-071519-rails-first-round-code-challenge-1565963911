<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to(heroine.super_name, heroine_path(heroine)) %>
    </li>
    <br>
  <% end %>
</ul>

<br> 
<br> 

<%= button_to "Create A Heroine", new_heroine_path, method: :get %><br> 
<%= button_to "List of Powers", powers_path, method: :get %>
